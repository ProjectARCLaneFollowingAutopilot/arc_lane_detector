#include <iostream>

using namespace std;

int main()
{

	// Read in an image.

	// Define manually where the two lane boundaries are --> save rho and theta of the lines.

	// Create a Region around the two lines (Reactangle, parallel to line).

	// Assign all the points inside the two rectangles to two vectors.

	// Fit a polynomial to the left data set by using the RANSAC class and do the same for right.

	// Draw the polynomial in a high resolution onto the original image (only for the ROI).

	return 0;
}